import React from 'react';
import {Row} from "react-bootstrap";

const RestrictionsOnAccountTransactions = () => {
    return(
        <Row>
            ограничения операций по счету
        </Row>
    );
}

export default RestrictionsOnAccountTransactions;